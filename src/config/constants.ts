/** USDC uses 6 decimals */
export const USDC_DECIMALS = 6;

/** Player tokens use 18 decimals */
export const TOKEN_DECIMALS = 18;

/** ESP token uses 18 decimals */
export const ESP_DECIMALS = 18;

/** Default slippage percentage (0.5%) */
export const DEFAULT_SLIPPAGE_PERCENT = 0.5;

/** Available slippage presets */
export const SLIPPAGE_OPTIONS = [0.5, 1, 2];

/** Default transaction deadline in seconds (5 minutes) */
export const DEFAULT_DEADLINE_SECONDS = 300;

/** Fee denominator used in contracts */
export const FEE_DENOMINATOR = 100000;

/** Basis points denominator */
export const BASIS_POINTS_DENOMINATOR = 10000n;

/** Zero address */
export const ZERO_ADDRESS = '0x0000000000000000000000000000000000000000';
